apiVersion: apps/v1
kind: Deployment
metadata:
  name: result-v2-deployment
  namespace: mightycapstone
  labels:
    app: result-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: result-v2
  template:
    metadata:
      labels:
        app: result-v2
    spec:
      containers:
      - name: result
        image: 255945442255.dkr.ecr.ap-southeast-1.amazonaws.com/mightycapstonevoting:result-v2
        ports:
        - containerPort: 80
          name: http
        env:
        - name: REDIS_HOST
          value: redis-service
        - name: DB_HOST
          value: db-service
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 20
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
